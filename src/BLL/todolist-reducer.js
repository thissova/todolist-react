let initalState = {
    tasks: {
        "hello": "world",
        'foo': "bar"
    }
}
const ADD_TASK = "ADD_TASK"
let todolistReducer = (state= initalState, action) => {
    return state
}

export default todolistReducer